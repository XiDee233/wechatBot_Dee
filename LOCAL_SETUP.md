# 本地部署与版本管理

此仓库基于 https://github.com/fanyuantaier/wechatbot-new ，原始版本 2.2.5，提交 5b85e3e。保留原项目 GPL 许可证。

## 启动

Windows 下使用 Python 3.11 创建 `.venv311`，安装 `requirements-installed.txt` 中的依赖，然后双击 `启动配置界面.bat`。首次启动会复制 `config.example.py` 为本地配置，并复制 `examples/prompts` 中的默认提示词。

配置页面：http://127.0.0.1:5000/ 。在正常 Windows 桌面运行并保持微信登录。

## 本地改动

- 网页通过当前 Python 解释器启动机器人，避免使用缺少依赖的系统 Python。
- 模型列表显示真实 DeepSeek 模型名称。
- 主聊天增加 DeepSeek 官方思考模式开关，保存后动态读取，只发送最终回答。
- 独立启动入口绑定本机地址，不结束占用端口的其他程序。

## 版本记录

`git status` 查看改动；`git diff` 查看差异；`git add 文件名` 后用 `git commit -m "说明"` 保存一个版本。

真实 `config.py`、个人 `prompts`、聊天记录、记忆、日志、备份和 Python 环境均不跟踪。Git 备份不会包含这些私人数据，请单独妥善保管；不要强制添加这些文件。


## 按需查询群历史与附件

在 Chat 模型配置启用“按需查询本群历史记录和附件”（默认开启），然后启动机器人。仍需满足当前群的 @ / 关键词 / 回复概率设置。示例：

- “10天前甲说了啥？”
- “总结昨天这个群的聊天。”
- “昨天甲发的图片里写了什么？”
- “找9月9日甲发的方案文件，概括内容。”

查询绑定当前已监听的群，模型不能指定其他群或私聊。日期按北京时间计算，结束日期包含当天。昵称有重名时先明确成员身份。回答带记录编号、时间和引用；只有本机保存的记录可查，无结果不代表没有发生过。

单次查询日期跨度最多31天，扫描最多5000条，返回最多80条/14000字；超限会标明截断。一轮最多3次检索、3个附件及5轮模型请求。工具调用中的历史正文和附件内容仅是数据，不作为执行指令。

图片和表情识别共用主聊天的 API 地址、Key、模型及思考模式。已移除独立的 Moonshot 配置。2026-09-19实测：当前 `deepseek-chat` 接口返回 `deepseek-flash` 并支持图像输入。依据官方文档：https://api-docs.deepseek.com/guides/vision 。不自动改写用户模型名。

图片仅读取本地原图或缩略图，无法读取时需先在电脑微信中打开。文件需本地已下载，按原消息MD5校验，避免误读其他群的同名文件；缺少校验信息则不读取。支持文本/代码、PDF（前30页文本，不含扫描件OCR）、DOCX、XLSX、PPTX的文字提取，最多10MB/14000字。不执行文件、宏或公式；语音和视频只显示消息类型，不转写。附件缓存 `history_cache/` 不入 Git。

验证命令：`.venv311\Scripts\python.exe -m unittest discover -s tests -v`。测试用模拟数据库/消息，不发送微信消息。


## 历史并发读取与回复格式修复

- 历史查询使用进程独立的解密缓存，与实时监听隔离。已在真实本机数据库复现旧共享缓存的WinError 2/5，并验证隔离后并发查询成功。
- 历史工具和主聊天API不增加失败重试；工具失败保留异常类型和完整日志，不把失败当成“没有记录”。
- 一次完整回复只保留首个角色署名；后续段落不重复，代码块不受影响。不追加固定“依据本机当前群记录”尾注，只有截断结果才注明覆盖不完整。
- 当前私人角色Prompt的旧“分段/撤回”规则已调整，原文件保存在 `backups/reply-style-before/`，不入Git。

## 多步骤 Agent

Agent 循环参考 OpenCode 的 session/tool 结构：模型输出工具调用，程序执行并把结构化结果追加回同一轮消息，模型继续决定下一步；只有没有工具调用时才形成最终回复。默认最多 10 步，相同工具和完全相同参数连续 3 次会停止。工具失败不会自动重试。

当前群可用工具包括：

- 最近上下文搜索：理解“他、这个、刚才、上面的人”，可从 60 分钟逐步扩大到 120 分钟。
- 指定日期搜索：处理“10 天前甲说了什么”。
- 跨时间文字搜索：日期未知时用多个短关键词搜索、查看命中前后消息并通过游标翻页。例如“之前那个什么鸭多少钱”。
- 附件浏览：按图片/文件、扩展名、文件名、成员和游标查找。例如“之前那个 Word 文档在哪”。
- 批量历史识图：每批最多 8 张，使用主模型定位“黄色鸭子照片”；本批没有且仍有更早图片时继续翻页。
- 附件读取：读取已定位的图片、文本、代码、PDF、DOCX、XLSX、PPTX。
- 群成员 @：底层提供真实 @ 能力，是否调用由角色 Prompt 和 Agent 判断；成员必须唯一。Agent 的中间步骤不会发微信，最终回复作为一条真实 @ 消息发送。
- 群成员目录：Agent 可列出当前群成员并解析昵称、备注、微信ID、群主和当前登录账号。成员身份、简称、别名、人物比较和“列出所有人”问题必须查目录，不再从旧助手回答中猜测。用户本轮明确纠正的别名关系优先。

真实 @ 使用成员的微信昵称（优先于本机备注），先单独输入 `@` 打开弹窗，再输入昵称过滤并选择唯一结果。模型输出中手写的 `@名字` 会在发送前移除，避免与微信生成的真实 mention 重复。结构化 @ 动作只执行一次，不做发送重试。

用户明确给出名字时，该名字原样输入微信 @ 搜索框，不再被数据库昵称替换。模型即使忘记调用工具，只要最终回复以 `@名字` 开头，底层也会自动提升为真实 @ 动作。若微信没有出现唯一候选，本次动作失败且不会把普通文字 `@名字` 发送出去冒充成功。

主消息监听也使用每个进程独立的解密数据库缓存。禁止监听进程、历史工具和诊断脚本共用 `%TEMP%/wechatauto_db/<账号>`，否则不同进程会同时替换 `.db/.tmp` 文件，产生持续 `PermissionError`，表现为微信新消息没有进入关键词判断。

配置页可调整 Agent 最大步骤（3–20）和重复调用停止阈值（2–5）。所有历史工具限定当前监听群，并使用与实时监听隔离的本地数据库快照。
